DOGE Forecast (ML2)

This is a Streamlit dashboard that predicts the next-day DOGE-USD close using quantile regression. It shows a p10, p50, and p90 forecast, plus a backtest chart on the last portion of historical data. This project is for learning and demo purposes and is not financial advice.

To run locally, install requirements and run `streamlit run app.py`.
